{
  "name": "@kong-ui/cli",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "main": "./bin/index.js",
  "scripts": {
    "create-package": "pnpm run --silent build:cli && node --experimental-specifier-resolution=node ./bin/index.js",
    "build:cli": "rimraf ./bin && tsc --project './tsconfig.json' && chmod u+x ./bin/index.js",
    "prepare": "pnpm run build:cli",
    "typecheck": "vue-tsc -p './tsconfig.build.json' --noEmit"
  },
  "repository": "https://github.com/Kong/kong-ui-shared-components/tree/main/packages/cli",
  "author": "Kong, Inc.",
  "license": "UNLICENSED",
  "volta": {
    "extends": "../../package.json"
  }
}
